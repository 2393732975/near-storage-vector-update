# PPT 基线复现实验报告（2026-09-18）

## 结论

已完成 4 个数据集、两种距离计算路径共 8 组基线实验。计算节点路径在 GIST1M、Text2Image10M 与 Deep100M 上吞吐更高；OSD CLS 路径不回传候选向量，消除了 118 MB–3.50 GB 的跨网络向量传输。SIFT100M 中 OSD 吞吐高 6.9%，但平均延迟仍高 25.4%。因此，本轮结果支持“算子卸载显著减少数据移动”，但尚不能以吞吐优势证明 OSD 基线优于计算节点基线。

## 可复核条件

- 代码：`36a9a1e`（含 `.u8bin` 文件头解析与更新偏移修复）。
- 运行器：`scripts/run-ppt-baselines.sh`；时间窗 300 s，`UPDATE_PARALLELISM=4`，每个模式/数据集使用新建的 `nsvu_*` 单副本池。
- 原始 JSON（未纳入 Git）：`results/ppt-baseline-20260917T101500Z/`。
- 对照来源：`docs/proposal-defense.pptx` 的计算节点/OSD CLS 基线页。

## 本次更新结果

吞吐为 `vectors_processed / graph_seconds`；平均延迟为成功与失败尝试的观测平均值。括号内为失败更新数。

| 数据集 | OSD 吞吐（upd/s） | 计算节点吞吐（upd/s） | OSD 平均延迟（ms） | 计算节点平均延迟（ms） | 计算节点回传向量 |
| --- | ---: | ---: | ---: | ---: | ---: |
| GIST1M | 6.439 (5) | 7.272 (12) | 449.9 | 263.5 | 3.50 GB |
| Text2Image10M | 5.409 (9) | 7.678 (10) | 451.3 | 293.5 | 833.4 MB |
| Deep100M | 5.176 (8) | 5.615 (11) | 514.7 | 366.7 | 341.6 MB |
| SIFT100M | 5.979 (5) | 5.592 (10) | 488.7 | 389.7 | 118.3 MB |

OSD 路径的 `remote_vector_calls` 与 `remote_vector_bytes` 均为 0；计算节点路径分别发生 0.79–1.00 百万次候选向量读取。OSD 侧的候选距离由已部署的 `hnsw_global` CLS 完成。

## 与答辩 PPT 的对照

| 数据集 | PPT OSD / 计算节点吞吐 | 本次 OSD / 计算节点吞吐 | 变化（OSD / 计算节点） |
| --- | ---: | ---: | ---: |
| GIST1M | 4.24 / 4.78 | 6.44 / 7.27 | +51.9% / +52.1% |
| Text2Image10M | 4.48 / 5.43 | 5.41 / 7.68 | +20.7% / +41.4% |
| Deep100M | 5.66 / 5.39 | 5.18 / 5.61 | -8.6% / +4.2% |
| SIFT100M | 5.15 / 5.45 | 5.98 / 5.59 | +16.1% / +2.6% |

趋势总体一致：前三个数据集里计算节点基线的吞吐更高，OSD CLS 的主要价值是避免大量候选向量传输。Deep100M 的相对快慢与 PPT 相反，表明该差异对当时的负载、队列与池放置敏感。

## 基础索引导入耗时

| 数据集 | OSD 前导入 | 计算节点前导入 |
| --- | ---: | ---: |
| GIST1M | 171.7 s | 159.3 s |
| Text2Image10M | 618.7 s | 587.9 s |
| Deep100M | 4695.3 s | 4714.2 s |
| SIFT100M | 4282.8 s | 4630.1 s |

## 限制与后续工作

- 八组运行均由 300 秒时间窗停止；存在 5–12 次失败更新，需先定位失败原因并使失败数归零，再用于最终论文结论。
- 计时在时间窗检查点停止，末批并发操作会使实际 `graph_seconds` 落在约 270–313 秒，应以 JSON 的吞吐字段比较，不应机械按 300 秒换算。
- 本轮 SIFT 使用修正后的 `.u8bin` 文件头解析，且采用隔离池和新一轮负载；数值可与 PPT 对照，但不应宣称为逐位一致复现。
